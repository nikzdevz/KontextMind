# Scripts in this directory are for Windows/macOS/Linux setup
# Copy to root of project before deploying

# Windows
setup.ps1
uninstall.ps1

# macOS/Linux
setup.sh
uninstall.sh

# Copy to project root and run:
# Windows:   .\setup.ps1
# macOS/Linux: chmod +x ./setup.sh && ./setup.sh